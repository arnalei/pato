/*
Deployment script for DausanDatabase

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "DausanDatabase"
:setvar DefaultFilePrefix "DausanDatabase"
:setvar DefaultDataPath "C:\Users\User\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\User\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Starting rebuilding table [dbo].[Product]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Product] (
    [ProductCode]  NVARCHAR (MAX) NOT NULL,
    [ProductName]  NVARCHAR (50)  NULL,
    [ProductPrice] NVARCHAR (50)  NULL,
    PRIMARY KEY CLUSTERED ([ProductCode] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Product])
    BEGIN
        INSERT INTO [dbo].[tmp_ms_xx_Product] ([ProductCode], [ProductName], [ProductPrice])
        SELECT   [ProductCode],
                 [ProductName],
                 [ProductPrice]
        FROM     [dbo].[Product]
        ORDER BY [ProductCode] ASC;
    END

DROP TABLE [dbo].[Product];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Product]', N'Product';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Altering Procedure [dbo].[createProduct]...';


GO
ALTER PROCEDURE [dbo].[createProduct]
	@ProductCode VARCHAR(MAX),
	@ProductName VARCHAR(50),
	@ProductPrice VARCHAR(50)
AS
	BEGIN
	INSERT INTO [dbo].[Product](ProductCode, ProductName, ProductPrice) VALUES(@ProductCode ,@ProductName ,@ProductPrice);
	END;
GO
PRINT N'Altering Procedure [dbo].[delete]...';


GO
ALTER PROCEDURE [dbo].[delete]
	@ProductCode VARCHAR(MAX)
AS
	BEGIN
	DELETE FROM [dbo].[Product] WHERE [ProductCode] = @ProductCode;
	END;
GO
PRINT N'Altering Procedure [dbo].[findProductById]...';


GO
ALTER PROCEDURE [dbo].[findProductById]
	@ProductCode VARCHAR(MAX)
AS
BEGIN
	Select [ProductCode] as ProductCode, [ProductName] as ProductName, [ProductPrice] as ProductPrice from [dbo].[Product] where [ProductCode] = @ProductCode
	END;
GO
PRINT N'Altering Procedure [dbo].[update]...';


GO
ALTER PROCEDURE [dbo].[update]
	@ProductCode VARCHAR(MAX),
	@ProductName VARCHAR(50),
	@ProductPrice VARCHAR(50)
AS
	BEGIN
	Update [dbo].[Product] set [ProductName] = @ProductName, [ProductPrice] = @ProductPrice Where [ProductCode] = @ProductCode
	END;
GO
PRINT N'Refreshing Procedure [dbo].[displayProduct]...';


GO
EXECUTE sp_refreshsqlmodule N'[dbo].[displayProduct]';


GO
PRINT N'Update complete.';


GO
